
import React from 'react';
import { Button } from 'react-bootstrap';

const EditAllocation = () => {
  const handleEditAllocation = () => {
    // Logic for editing resource allocation
  };

  return (
    <Button variant="primary" onClick={handleEditAllocation}>
      Edit Allocation
    </Button>
  );
};

export default EditAllocation;
