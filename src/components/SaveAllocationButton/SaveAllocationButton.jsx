
import React from 'react';
import { Button } from 'react-bootstrap';

const SaveAllocationButton = () => {
  const handleSave = () => {
    // Logic for saving allocation details
  };

  return (
    <Button variant="primary" onClick={handleSave}>
      Save Allocation
    </Button>
  );
};

export default SaveAllocationButton;
